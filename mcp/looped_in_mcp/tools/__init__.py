"""Tool modules — one per domain.

Each module exposes `register(mcp, deps)` and is listed in `registry.py`; nothing
is auto-discovered, so the whole tool surface stays greppable.
"""

from __future__ import annotations
